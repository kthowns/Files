1. GUI
 -> API 사용 (AWT, Swing)
  --> AWT : 플랫폼 의존적, 무겁고 look and feel 지원x
  --> Swing : 플랫폼 독립적, 가볍고 look and feel 지원o
  #JAVA 11 이상 버전에서 실습 시 모듈화 문제로 Import 에러 발생
   -> module-info.java 파일에 requires deskstop; requires sql; 내용 추가하면 해결

<버튼 생성 및 배치관리자(Layout)>
package test5;

import javax.swing.*;
import java.awt.*;

public class test5 extends JFrame {
	public test5() {
		setSize(300,200);
		setTitle("MyFrame");
		setLayout(new FlowLayout());
		setLayout(new BorderLayout());
		
		JButton button = new JButton("Button");
		JButton b2 = new JButton("But2");
		
		add(button, BorderLayout.CENTER);
		add(b2, "East");
		
		setVisible(true);
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	}
	
	public static void main(String[] args) {
		test5 f = new test5();
	}
}
